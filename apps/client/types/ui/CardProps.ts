import { CommentType } from '@/components/comment/comment'

export type CardProps = {
    order: number
    book: string
    author: string
    cover: string
    title: string
    createdAt: string
}
